# 测试数据说明

本目录下的 OCR 结果文件（`000101/ocr_result/*.json`）是**合成、脱敏的测试数据**，
仅用于单元测试与离线演示，不含任何真实个人隐私信息：

- **姓名**：均为占位名（张三、王五、李四、马超、黄磊等），非真实个人信息；
- **身份证号**：中间位已用 `***` 打码（如 `110101***001`）；
- **地址**：仅保留到区县级（如「门头沟区」「东城区」），不含完整门牌号。

`tests/0001.json` 同样为合成样例，地址使用「示例路」「XX 区」等占位符。
